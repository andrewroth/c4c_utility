defaults: &defaults
  adapter: mysql
  username: root
  password: 
  host: localhost

development:
  database: local.intranet.prod
  <<: *defaults

test:
  database: local.intranet.test
  <<: *defaults

production:
  database: local.intranet.prod
  <<: *defaults

ciministry_development:
  database: local.intranet.prod
  <<: *defaults

ciministry_production:
  database: local.intranet.prod
  <<: *defaults
