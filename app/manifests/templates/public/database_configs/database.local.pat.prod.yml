defaults: &defaults
  adapter: mysql
  username: root
  password: 
  host: localhost

development:
  database: local.pat.prod
  <<: *defaults

test:
  database: local.pat.test
  <<: *defaults

production:
  database: local.pat.prod
  <<: *defaults

ciministry_development:
  database: local.intranet.prod
  <<: *defaults

ciministry_production:
  database: local.intranet.prod
  <<: *defaults
