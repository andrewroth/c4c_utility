defaults: &defaults
  adapter: mysql
  username: root
  password: 
  host: localhost

development:
  database: local.pat.dev
  <<: *defaults

test:
  database: local.pat.test
  <<: *defaults

production:
  database: local.pat.dev
  <<: *defaults

ciministry_development:
  database: local.intranet.dev
  <<: *defaults

ciministry_production:
  database: local.intranet.dev
  <<: *defaults
